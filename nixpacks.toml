[phases.setup]
nixPkgs = ["...", "glib", "nss", "nspr", "atk", "at-spi2-atk", "libdrm", "expat", "libxkbcommon", "mesa", "alsa-lib", "pango", "cairo"]

[phases.build]
cmds = [
  "pip install -r requirements.txt",
  "playwright install --with-deps chromium"
]

[start]
cmd = "python main.py"
